@using PagedList
@using PagedList.Mvc
@using Control.Data.Identity
@model IPagedList<ApplicationUser>
@{
    ViewBag.Title = "İdareci İşlemleri";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section id="main-content">
    <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        @ViewBag.Title || <a href="~/Admin/Index" class="btn btn-primary btn-xs">Admin Anasayfası</a> || <a href="~/AutorAccount/Register" class="btn btn-success btn-xs">İdareci Ekle</a> || <a href="~/AutorAccount/RegisterAsistants" class="btn btn-default btn-xs">İdareci Asistanı Ekle</a> || <a href="~/AutorAccount/RegisterHelpers" class="btn btn-info btn-xs">İdareci Yardımcısı Ekle</a>
                    </header>
                    <div class="panel-body">
                        <div class="adv-table">
                            <table class="display table table-bordered table-striped" id="example">
                                <thead>
                                    <tr>
                                        <th>Adı-Soyadı</th>
                                        <th>Kullanıcı Adı</th>
                                        <th>Görevi</th>
                                        <th>Doğum Tarihi - Başlama Tarihi</th>
                                        <th>Cinsiyeti</th>
                                        <th>Mail Adresi</th>
                                        <th>Telefon No</th>
                                        <th>Ülkesi - Şehir</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr class="gradeX">
                                            <td>@item.NameLastname</td>
                                            <td>@item.UserName</td>
                                            <td>@item.RespondTitle</td>
                                            <td>@item.Birthdate - @item.HireDate</td>
                                            <td>@item.Gender</td>
                                            <td>@item.Email</td>
                                            <td>@item.PhoneNumber</td>
                                            <td>@item.Country - @item.City</td>
                                            <td>
                                                <a href="~/AutorAccount/ChangePassword" class="btn btn-success btn-xs">Şifre Değiştir</a>
                                                <a href="~/Author/Delete" class="btn btn-danger btn-xs">Sil</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <th>Adı-Soyadı</th>
                                        <th>Kullanıcı Adı</th>
                                        <th>Doğum Tarihi - Cinsiyeti</th>
                                        <th>Doğum Tarihi - Başlama Tarihi</th>
                                        <th>Mail Adresi</th>
                                        <th>Telefon No</th>
                                        <th>Ülkesi - Şehir</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </tfoot>
                            </table>
                            <div class="pagination">
                                <div class="paginate_active">
                                    @Html.PagedListPager(Model, Page => Url.Action("Author", new { Page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </section>
</section>
