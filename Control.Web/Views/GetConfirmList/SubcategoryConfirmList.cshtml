@using PagedList
@using PagedList.Mvc
@using Control.Entities.Models.Entites
@model IPagedList<Subcategory>
@{
    ViewBag.Title = "Altkategori Onay Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section id="main-content">
    <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        @ViewBag.Title || <a href="~/Subcategory/Index" class="btn btn-primary btn-xs">Altkategori Listesi</a>
                    </header>
                    <div class="panel-body">
                        <div class="adv-table">
                            <table class="display table table-bordered table-striped" id="example">
                                <thead>
                                    <tr>
                                        <th>Alkategori Adı</th>
                                        <th>Kategori Adı</th>
                                        <th>Ürün Sayısı</th>
                                        <th>Eklenme Tarihi</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr class="gradeX">
                                            <td>@item.Name</td>
                                            <td>@item.Category.Name</td>
                                            <td>@item.Products.Count()</td>
                                            <td>@item.CreatedTime.ToShortDateString()</td>
                                            <td>
                                                <a href="~/GetConfirmList/SubcategoryConfirm/@item.Id" class="btn btn-success btn-xs">Onayla</a>
                                                <a href="~/Subcategory/Delete/@item.Id" class="btn btn-danger btn-xs">Sil</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <th>Alkategori Adı</th>
                                        <th>Kategori Adı</th>
                                        <th>Ürün Sayısı</th>
                                        <th>Eklenme Tarihi</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </tfoot>
                            </table>
                            <div class="pagination">
                                <div class="paginate_active">
                                    @Html.PagedListPager(Model, Page => Url.Action("SubcategoryConfirmList", new { Page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </section>
</section>
